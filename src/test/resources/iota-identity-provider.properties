# public facing authentication endpoint
authentication.endpoint=https://localhost:8443/realms/iota/authenticate

# internal sidecar url, only called by identity-provider
sidecar.url=http://localhost:7001

# skip actual verification of credentials in tests
verification.skip=true